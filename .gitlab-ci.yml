stages:
  - build
  - test
  
build:
  stage: build
  script:
    - echo "build"

test:
  stage: test
  script:
    - echo "test"
    
test-sonar:
  stage: test
  script:
    - /opt/sonar-scanner/bin/sonar-scanner -Dsonar.projectKey=GitlabDeveloper_knowledgestore-front -Dsonar.organization=gitlabdeveloper-github -Dsonar.sources=. -Dsonar.host.url=https://sonarcloud.io -Dsonar.login=ec8b6fc545a9584954910aeb45f4162bec0f5c6a
    