
const INITIAL_STATE = {
	//TODO
	isLoading: false,
	errorMsg: null,
};

const authReducer = (state = INITIAL_STATE, {type, payload}) => {
	switch (type) {
		//TODO
		default:
			return state;
	}
};

export default authReducer;